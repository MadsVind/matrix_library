#include <matrixTest.hpp>

// ! don't care not done

TEST_CASE("", "[benchmark]") {
    Matrix<double> matrix1;

    matrix1.addRow({1, 2, 3})
           .addRow({4, 5, 6})
           .addRow({7, 8, 9});

}

TEST_CASE("", "[benchmark]") {
    Matrix<double> matrix1;

    matrix1.addRow({1, 2, 3})
           .addRow({4, 5, 6})
           .addRow({7, 8, 9});

   
}

TEST_CASE("", "[benchmark]") {
    Matrix<double> matrix1;

    matrix1.addRow({1, 2, 3})
           .addRow({4, 5, 6})
           .addRow({7, 8, 9});

    
}